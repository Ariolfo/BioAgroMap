import ClientLandingPage from "./ClientLandingPage";

/** Ruta admin dedicada: edición de narrativa + vista previa como cliente. */
export default function AdminLandingPage() {
  return <ClientLandingPage mode="admin-edit" />;
}
